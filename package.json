{
  "name": "trello-assistant-backend",
  "version": "1.0.0",
  "description": "Backend pour assistant Trello via OpenAI function calling",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "postbuild": "echo '✅ Build terminé' && test -d dist && echo '✅ dist/ existe' && ls -la dist/ | head -5 || (echo '❌ dist/ introuvable après build' && exit 1)",
    "start": "node start.js",
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "test": "jest"
  },
  "keywords": ["trello", "openai", "assistant"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "axios": "^1.6.2",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.5",
    "@types/cors": "^2.8.17",
    "typescript": "^5.3.3",
    "ts-node-dev": "^2.0.0",
    "jest": "^29.7.0",
    "@types/jest": "^29.5.11",
    "ts-jest": "^29.1.1"
  }
}

